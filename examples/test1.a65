; program to load a value from the stack without using PL.. instructions
; this program only will terminate with the Y register containing the
; value that was pushed onto the stack
tsx
lda #$A1
pha

ldy $0100,X
